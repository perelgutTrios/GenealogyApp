# Environment Variables
NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb://localhost:27017/genealogy-db
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=30d

# Email Configuration (configure with your email service)
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Encryption
ENCRYPTION_KEY=your-32-character-encryption-key

# Client URL
CLIENT_URL=http://localhost:3000

# Trust Proxy configuration (controls Express 'trust proxy')
# Defaults to ['loopback','linklocal','uniquelocal'] if unset.
# Examples:
# TRUST_PROXY=true
# TRUST_PROXY=false
# TRUST_PROXY=1
# TRUST_PROXY=loopback,linklocal,uniquelocal
# TRUST_PROXY=127.0.0.1
TRUST_PROXY=

# AI Providers (choose at least one)
# Prefer GEMINI_API_KEY for free-tier development; OpenAI will be used as fallback if both are set
GEMINI_API_KEY=your_gemini_api_key_here
# Optional: override the Gemini model (defaults to gemini-1.5-flash-latest)
GEMINI_MODEL=gemini-1.5-flash-latest
# Optional: enable Google Search grounding for Gemini (v1beta). Default: false
GEMINI_ENABLE_WEB_GROUNDING=false
OPENAI_API_KEY=your_openai_api_key_here

# External API Integrations
# FamilySearch (highly recommended)
FAMILYSEARCH_CLIENT_ID=your_familysearch_client_id_here
FAMILYSEARCH_CLIENT_SECRET=your_familysearch_client_secret_here

# WikiTree (no key required for basic usage)
# Optional overrides for the public API endpoint and an app identifier label
WIKITREE_API_BASE=https://api.wikitree.com/api.php
WIKITREE_APP_ID=GenealogyApp

# Chronicling America (Library of Congress)
CHRONICLING_AMERICA_API_BASE=https://chroniclingamerica.loc.gov

# Optional premium providers (leave blank if not used)
BILLIONGRAVES_API_KEY=
MYHERITAGE_API_KEY=

# Mock sources toggle (default: false)
# When true, enables any mock data sources wired into the app (e.g., FindAGrave/Newspaper mocks).
# Production and normal development should keep this disabled.
ENABLE_MOCK_SOURCES=false